import React, { Component } from 'react'
import LocalClint from '../LocalClint'

export default class Products extends Component {
    render() {
        return (
            <div>
                <LocalClint/>
            </div>
        )
    }
}
